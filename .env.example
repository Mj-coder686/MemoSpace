# 复制为 .env 后再修改。以下只是本地开发默认值，不应用于公网生产环境。
MYSQL_DATABASE=memospace
MYSQL_USER=memospace
MYSQL_PASSWORD=memospace_db_2026
MYSQL_ROOT_PASSWORD=root_memospace_2026

REDIS_PASSWORD=memospace_redis_2026

MINIO_ROOT_USER=memospace_minio
MINIO_ROOT_PASSWORD=memospace_minio_2026
MINIO_BUCKET=memospace-private

JWT_SECRET=MemoSpace_Local_Development_Secret_Change_Me_2026_At_Least_32_Bytes
FLYWAY_BASELINE_ON_MIGRATE=true
DEMO_DATA_ENABLED=false
ADMIN_ENABLED=true
# 多个管理员用分号分隔，每项格式：用户名|密码|昵称。填写后不会再创建下方的单个兼容账号。
ADMIN_ACCOUNTS=
ADMIN_USERNAME=admin
ADMIN_PASSWORD=MemoAdmin2026!
ADMIN_NICKNAME=拾光管理员
FRONTEND_PORT=3000
BACKEND_PORT=18081
# Android APK 默认从 https://localhost 页面运行；电脑局域网地址由原生 HTTP 通道访问。
CORS_ORIGINS=http://localhost:3000,https://localhost,capacitor://localhost

spring:
  datasource:
    url: ${DB_URL:jdbc:mysql://mysql:3306/memospace?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&useSSL=false}
    username: ${DB_USERNAME:memospace}
    password: ${DB_PASSWORD:memospace_db_2026}
    driver-class-name: com.mysql.cj.jdbc.Driver
  sql:
    init:
      mode: always

app:
  cache-enabled: true
  storage:
    mode: minio

spring:
  datasource:
    url: jdbc:h2:mem:memospace_test;MODE=MySQL;DATABASE_TO_LOWER=TRUE;DB_CLOSE_DELAY=-1
    username: sa
    password:
    driver-class-name: org.h2.Driver
app:
  demo-data:
    enabled: true
  reminders:
    initial-delay-ms: 3600000
  storage:
    mode: local
    local-path: ./target/test-uploads

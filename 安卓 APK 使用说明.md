# 拾光空间 Android 测试版

这个 APK 是 MemoSpace 的手机入口，**不会创建另一套数据库**。手机登录、好友、共享空间、图片、通知和聊天，都使用这台电脑上 Docker 正在运行的同一套后端、MySQL、Redis 与 MinIO。

## 安装

1. 把 `MemoSpace-Android-test.apk` 传到安卓手机（微信文件传输、QQ、数据线都可以）。
2. 在手机文件管理器中点开 APK；若系统提示，允许本次来源安装未知应用。
3. 安装后打开“拾光空间”。支持 Android 7.0（API 24）及以上系统。

## 第一次连接电脑上的服务

1. 确保电脑和手机连接同一个 Wi-Fi，且 Docker Desktop 中的 `memo-space` 项目已启动。
2. 在电脑打开 PowerShell，输入 `ipconfig`，找到当前 Wi-Fi 的 **IPv4 地址**，例如 `192.168.1.8`。
3. 在 APK 登录页展开“共享服务器”，填入 `http://192.168.1.8:18081`（把示例 IP 换成电脑的实际 IPv4 地址），然后点“保存连接”。
4. 使用已有账号登录即可。手机和网页端看到的是同一份数据。

不要填 `localhost` 或 `127.0.0.1`：在手机上它们表示手机自己，并不是运行 Docker 的电脑。

## 如果手机连不上

- 先确认电脑网页可打开 `http://localhost:3000`，并确认 Docker Desktop 中后端容器正在运行。
- 再确认手机和电脑不在访客 Wi-Fi、也没有连接不同网络。
- Windows 首次拦截时，请只允许专用网络访问 Docker/Java 的本地服务；不要在公共网络中开放。
- 后端配置更新后，请在 Docker Desktop 中重新启动 `memo-space` 项目，使新的手机端跨域设置生效。

## 日后部署到公网

将 Docker 服务迁移到带 HTTPS 的公网域名后，只需在这里填写例如 `https://memo.example.com`。APK 与网页会继续共用同一套数据，无需为安卓单独部署数据库或 MinIO。

## 这次测试包的性质

这是开发测试签名包，便于直接体验和迭代。正式对外分发前，会使用固定发布签名重新生成；届时同一签名才能覆盖安装更新版本。
